## 1. 数据层重构

- [x] 1.1 在 `src/services/aiConfig.ts` 中定义 `AIConfigProfile` 和 `AIConfigStore` 接口
- [x] 1.2 实现 `loadAIConfigStore()` / `saveAIConfigStore()` 读写新 localStorage key
- [x] 1.3 实现旧配置自动迁移逻辑（检测旧 key → 转换 → 写入新 key → 标记 `_migrated`）
- [x] 1.4 重构 `loadAIConfig()` 内部实现为读取活动配置，保持返回签名不变
- [x] 1.5 新增 `getActiveProfile()` / `setActiveId()` / `addProfile()` / `updateProfile()` / `removeProfile()` / `listProfiles()` 等 CRUD 函数
- [x] 1.6 实现配置名称自动生成逻辑（`{Provider} / {model}` 格式）

## 2. Settings 面板改造

- [x] 2.1 新增列表视图组件：展示所有配置卡片（名称、provider、model、活动标记）
- [x] 2.2 列表视图操作按钮：设为活动、编辑、删除（含确认）
- [x] 2.3 新增「新增配置」按钮，点击进入编辑视图（空白表单）
- [x] 2.4 编辑视图复用现有表单，新增名称字段，保存后回到列表视图
- [x] 2.5 配置上限（10 条）检查与禁用提示
- [x] 2.6 空列表时显示 GLM 推荐横幅（复用现��横幅逻辑，调整触发条件）

## 3. 主界面快速切换

- [x] 3.1 在 ResultsPage 报告生成按钮附近新增配置快速切换下拉组件
- [x] 3.2 下拉展示所有配置名称，选中即切换 activeId（不触发报告重新生成）
- [x] 3.3 仅在存在 2+ 配置时显示快速切换下拉

## 4. 重试时切换配置

- [x] 4.1 报告生成失败后的重试 UI 中增加配置选择下拉
- [x] 4.2 用户选择另一配置后点击重试，使用所选配置发起请求

## 5. 国际化

- [x] 5.1 在 `settings.json`（zh/en）中新增多配置相关文案（新增、编辑、删除、设为活动、配置名称、已达上限等）
- [x] 5.2 在 `results.json`（zh/en）中新增快速切换与重试切换相关文案

## 6. 验证

- [x] 6.1 验证旧配置迁移：清除新 key，保留旧 key，刷新后确认自动迁移成功
- [x] 6.2 验证 CRUD 流程：新增、编辑、删除、切换活动配置
- [x] 6.3 验证快速切换：主界面下拉切换后生成报告使用新配置
- [x] 6.4 验证重试切换：失败后选择另一配置重试成功
- [x] 6.5 验证边界：删除活动配置后自动切换、达到上限禁用新增
